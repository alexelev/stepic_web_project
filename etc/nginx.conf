events {
	worker_connections 768;
	# multi_accept on;
}

http {
	server{
		listen	80;
		location ^~ /uploads/{
			root	/home/box/web/uploads/;
		location ~* \.\w+${
			root	/home/box/web/public/;
		}
		return 404;
		
		}

	}
}
